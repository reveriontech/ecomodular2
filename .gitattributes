# Removed LFS tracking for mp4/mov files to work with Vercel
# Vercel doesn't support Git LFS during builds
# If videos are too large, consider hosting them externally (CDN, Cloudinary, etc.)
# *.mov filter=lfs diff=lfs merge=lfs -text
# *.mp4 filter=lfs diff=lfs merge=lfs -text
